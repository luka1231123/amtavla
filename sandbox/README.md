# Local file sandbox

Put text files here when you want amtavla to list, find, or read them.

The assistant has read-only access to this folder. It cannot read paths outside
the sandbox, and it limits file sizes and search results before adding them to a
prompt.
